const ROOT_URL = '/admin/api?typename='

export default {
  // AUI Event API
  fetchEventList: ``

  createEvent: `${ROOT_URL}/CreateNewTour`, // POST

  updateEventStatus: `${ROOT_URL}/UpdateTourStatus`, // GET
}
